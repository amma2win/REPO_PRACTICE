import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const HomeClient = createSlice({
  name: 'hice este cambio',
  initialState,
  reducers: {}
});

export const {} = HomeClient.actions

export default HomeClient.reducer
